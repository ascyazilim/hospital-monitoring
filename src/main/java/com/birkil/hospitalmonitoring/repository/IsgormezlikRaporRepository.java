package com.birkil.hospitalmonitoring.repository;

import com.birkil.hospitalmonitoring.entity.IsgoremezlikRapor;
import org.springframework.data.jpa.repository.JpaRepository;

public interface IsgormezlikRaporRepository extends JpaRepository<IsgoremezlikRapor, Long> {
}
